Create both HTML pages
   Still need to do a footer

Create the routes between all pages
    -running

get both HTML pages working
    X

create a variable that will store/create the new user object
    X

create a function that will LOOP through all USERs and compare scores 

create an ALERT/MODULE or PROMPT that will SHOW the the active user who the best match is,

create another function that will store ALL the users score in a DATA base 

Create AJAX calls for data base

NPM packages = body-parser, path?, express

study body parser npm

obtain access to the friends.js file and friends object



